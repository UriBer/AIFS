# Core dependencies
grpcio==1.54.0
grpcio-tools==1.54.0
grpcio-reflection==1.54.0
protobuf==4.22.3
# Using hashlib (standard library) instead of blake3 to avoid Rust dependency
# blake3==0.3.3
numpy==1.24.3

# FAISS alternatives - try these in order:
# Option 1: Pre-built wheel (most compatible)
faiss-cpu>=1.7.4; platform_machine == "x86_64"
# Option 2: Conda-based (if pip fails)
# faiss-cpu>=1.7.4; platform_machine == "x86_64"
# Option 3: Pure Python alternative (fallback)
scikit-learn>=1.0.0  # For basic vector operations when FAISS unavailable

sqlite-utils==3.35
pydantic==2.0.3
typer>=0.10.0
rich==13.4.2

# Security and encryption dependencies
cryptography>=45.0.0  # For AES-256-GCM and Ed25519
pynacl>=1.5.0  # For Ed25519 signatures
macaroon>=0.1.0  # For capability-based authorization

# Compression support
zstandard>=0.22.0  # For zstd compression as required by spec

# Test dependencies
pytest
pytest-grpc  # For testing gRPC services
pytest-mock  # For mocking in tests

# Optional dependencies
fusepy==3.0.1  # For FUSE implementation